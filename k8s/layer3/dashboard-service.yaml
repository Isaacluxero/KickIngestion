apiVersion: v1
kind: Service
metadata:
  name: clip-dashboard-svc
  namespace: default
spec:
  selector:
    app: clip-dashboard
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
